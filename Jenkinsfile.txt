pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/THIYANESH1333/GUVI_DEVOPS.git'
            }
        }
        stage('Load Jenkinsfile') {
            steps {
                load 'pipeline/Jenkinsfile'
            }
        }
    }
}
